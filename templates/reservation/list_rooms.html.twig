<div class="pad100">
    {% for room in roomCollection %}
        <div class="row card-container shadow p-3 mb-5 bg-white">
            <div class="col-12">
                <div class="card card-reservation">
                    <div class="card-body">

                        <div class="row">
                            <div class="col-12 col-md-4 d-flex align-items-center mb-3 mb-md-0">
                                <h5 class="card-title">{{ room.getType() }}</h5>
                            </div>
                            <div class="col-12 col-md-6 card-text d-flex align-content-center mb-3 mb-md-0">
                                <div class="row">
                                    <div class="col-12">Precio</div>
                                    <div class="col-12 date-color">{{ room.getPrice() }} €</div>
                                </div>
                            </div>
                            <div class="col-12 col-md-2 d-flex align-content-center justify-content-center">
                                <button class="btn btn-secondary" data-toggle="modal" data-target="#exampleModal"
                                        data-whatever="@getbootstrap">Reservar
                                </button>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    {% endfor %}
</div>
